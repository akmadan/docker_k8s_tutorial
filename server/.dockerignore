# ============================================
# .dockerignore file
# ============================================
# Similar to .gitignore, this tells Docker which files/folders
# to exclude when building the image (COPY command)
# This reduces build context size and speeds up builds

# Dependencies (will be installed in container)
node_modules
npm-debug.log

# Environment files (should be set via environment variables)
.env
.env.local
.env.*.local

# Git files
.git
.gitignore

# IDE and editor files
.vscode
.idea
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Documentation (not needed in container)
README.md
*.md

# Docker files (not needed inside container)
Dockerfile
docker-compose.yml
.dockerignore
